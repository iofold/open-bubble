openapi: 3.1.0
info:
  title: Open Bubble API
  version: 0.1.0
  summary: Minimal contract for the local prompt MVP.
servers:
  - url: http://localhost:3000
    description: Local development server
paths:
  /health:
    get:
      summary: Check server health
      responses:
        '200':
          description: Server is reachable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /prompt:
    post:
      summary: Submit a media prompt and receive a synchronous answer
      description: Send one multipart file named `media` and an optional text prompt. The runtime accepts `image/*` and `video/*`; any other media type should return `415 Unsupported Media Type`.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PromptRequest'
      responses:
        '200':
          description: Prompt handled synchronously
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromptResponse'
        '400':
          description: Missing required media or invalid prompt payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '415':
          description: Unsupported media type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    HealthResponse:
      type: object
      required: [ok, service, version]
      properties:
        ok:
          type: boolean
        service:
          type: string
          const: open-bubble-api
        version:
          type: string
    PromptRequest:
      type: object
      required: [media]
      properties:
        media:
          type: string
          format: binary
          description: Upload one image/* or video/* file.
        prompt:
          type: string
          description: Optional text prompt sent with the uploaded media.
      additionalProperties: false
    PromptMedia:
      type: object
      required: [filename, mimeType, kind]
      properties:
        filename:
          type: string
        mimeType:
          type: string
        kind:
          type: string
          enum: [image, video]
    PromptResponse:
      type: object
      required: [answer, media, receivedAt]
      properties:
        answer:
          type: string
        prompt:
          type: string
        media:
          $ref: '#/components/schemas/PromptMedia'
        receivedAt:
          type: string
          format: date-time
    ErrorResponse:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
        message:
          type: string
