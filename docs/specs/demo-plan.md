# Demo Plan

1. Start the local API from `apps/api`.
2. Call `GET /health`.
3. Send one screenshot or screen recording plus prompt text and/or raw audio to `POST /prompt`.
4. Show the JSON response in the client.

If needed, keep the story focused on a single round trip and leave richer workflows for a later pass.
